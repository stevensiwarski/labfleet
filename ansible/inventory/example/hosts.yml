---
# Documentation-only placeholders. Intentionally not deployable.
all:
  children:
    labfleet_nodes:
      hosts:
        labfleet-example-01:
          ansible_host: NODE_ADDRESS_REQUIRED
          ansible_user: fleet
          labfleet_vm_id: 930007
          labfleet_proxmox_node: NODE_NAME_REQUIRED
          labfleet_expected_hostname: labfleet-example-01
    bootstrap_control:
      hosts: {}
  vars:
    ansible_connection: ssh
    # Configure a private known_hosts file and ProxyJump outside Git if needed.
    # Supply become credentials externally, e.g. --ask-become-pass; never here.
    labfleet_disposable: false
